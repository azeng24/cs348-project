# cs348-project
A sneaker inventory tracking app. Allows you to track sales and generate sales reports.

## To Run:
1. Activate venv: `source myenv/bin/activate`
2. Navigate to `shoe_inventory` directory
3. Use command `python3 manage.py runserver` to start server

If you need to go to the admin page, navigate to localhost:8000/admin
